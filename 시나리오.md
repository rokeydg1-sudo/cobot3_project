# Cobot3 Scenario 0

## 개요

Scenario 0는 Assembly Cell이 요청한 작업을 FMS가 관리하고, 유휴 AMR이
Pull 방식으로 작업을 한 건씩 할당받아 운송하는 구조이다.

```text
Assembly Cell
→ FMS 작업 큐
→ cuOpt 작업 순서 최적화
→ AMR에 Pickup/Delivery 좌표 할당
→ Nav2 경로 계획 및 주행 제어
→ Isaac Sim AMR 이동
→ AMR 상태 및 위치를 FMS에 보고
→ 완료 후 다음 작업 요청
```

## 1. Assembly Cell → FMS

Assembly Cell은 `/assembly/request`로 운송 작업을 요청한다. 현재 메시지는
`std_msgs/msg/String`을 사용하며 형식은 다음과 같다.

```text
cell_id=A,
task_id=task_01,
kit_id=KIT_STAR,
processing_time=3.0,
urgency=3
```

FMS는 메시지를 `Task`로 변환해 최대 10개의 대기 작업 큐에 저장한다.

```text
status=WAITING
delivery_cell=cell_a
```

- `requested_at`은 작업 요청이 FMS에 도착한 시각이다.
- 시간은 FMS 시작 시점을 0으로 한 상대시간을 사용한다.
- `processing_time`은 Assembly Cell의 가공시간이다.
- AMR은 `processing_time` 동안 Cell 앞에서 대기하지 않는다.

## 2. Isaac Sim → AMR Node

Isaac Sim은 `/amr/odom`으로 AMR 위치를 제공한다. AMR Node는 최초 odom을
받은 뒤 Isaac Sim과 통신할 준비가 됐다고 판단하고 다음 상태를 발행한다.

```text
state=IDLE
event=READY
load_state=EMPTY
```

AMR ID는 `main()`에서 결정하고 설정 문자열을 변경하지 않은 채 `AMRNode`
생성자에 전달한다.

```python
amr_id = "AMR_01"
node = AMRNode(amr_id)
```

## 3. AMR Node → FMS 작업 요청

AMR은 다음 조건을 모두 만족할 때 FMS에 다음 작업을 요청한다.

```text
Isaac Sim odom 수신 완료
state == IDLE
task_running == False
처리 중인 작업 요청이 없음
```

작업 요청은 `/fms/request_task` 서비스를 사용한다.

```text
amr_id
state
current_task_id
현재 x, y
load_state
```

이 구조는 FMS가 작업을 밀어 넣는 Push 방식이 아니라, 준비된 AMR이 작업을
가져가는 Pull 방식이다.

## 4. FMS의 AMR 상태 관리

FMS는 `amr_id`를 key로 사용해 AMR 대수만큼 상태 객체를 독립적으로
생성하고 관리한다.

```text
amr_states["AMR_01"]
amr_states["AMR_02"]
amr_states["AMR_03"]
```

각 AMR 상태에는 다음 값이 포함된다.

```text
state
event/status
current_task_id
kit_id
load_state
x
y
```

AMR의 작업 요청을 받으면 FMS는 다음 조건을 검증한다.

```text
amr_id가 존재함
state == IDLE
current_task_id가 비어 있음
load_state == EMPTY
FMS가 저장한 AMR 상태도 작업 가능한 상태임
```

## 5. FMS → cuOpt

대기 작업이 있으면 FMS는 현재 작업 큐 전체와 요청한 AMR의 현재 상태를
cuOpt에 전달한다.

cuOpt가 고려하는 항목은 다음과 같다.

```text
현재 AMR 위치
Parts Supermarket 위치
각 Delivery Cell 위치
작업 요청 시각
긴급도
Deadline
Assembly processing time
이동 거리와 예상 이동시간
Parts Supermarket 적재시간 2초
```

첫 번째 작업의 이동 비용은 다음 경로를 기준으로 한다.

```text
현재 AMR 위치
→ Parts Supermarket
→ 해당 Assembly Cell
```

연속 작업은 다음 경로를 기준으로 한다.

```text
이전 Assembly Cell
→ Parts Supermarket
→ 다음 Assembly Cell
```

cuOpt는 작업 순서를 결정한다. Nav2 경로나 실제 waypoint는 계산하지 않는다.

## 6. FMS의 작업 할당

FMS는 cuOpt가 반환한 최적 순서 중 첫 번째 작업을 요청한 AMR에 할당한다.
이때 논리 위치를 실제 물리 좌표로 변환한다.

```text
supermarket → (-7.0,  0.0)
cell_a      → ( 7.0,  3.5)
cell_b      → ( 7.0,  0.0)
cell_c      → ( 7.0, -3.5)
```

FMS는 `/fms/request_task` 응답으로 다음 데이터를 전달한다.

```text
task_id
kit_id
processing_time
pickup_id, pickup_x, pickup_y
delivery_id, delivery_x, delivery_y
```

합의된 정책에 따라 작업은 AMR에 응답하는 시점에 대기 큐에서 제거한다.

```text
WAITING queue
→ AMR 할당
→ queue에서 제거
```

## 7. AMR Node → Nav2 → Isaac Sim

AMR은 FMS가 반환한 Pickup 좌표를 `NavigateToPose` goal로 Nav2에 전달한다.

```text
AMR Node
  │ 목적지 Pose
  ▼
Nav2 NavigateToPose
  │
  ├─ Planner가 중간 경로(Path/waypoints) 생성
  ├─ Controller가 경로를 추종
  └─ /cmd_vel 발행
         │
         ▼
     Isaac Sim AMR
```

역할은 다음과 같이 구분한다.

- AMR Node는 FMS가 준 최종 목적지 Pose를 Nav2에 전달한다.
- Nav2 Planner는 현재 위치부터 목적지까지 중간 경로를 계산한다.
- Nav2 Controller는 경로를 추종하며 `/cmd_vel`을 생성한다.
- Isaac Sim은 `/cmd_vel`에 따라 Nova Carter를 이동시킨다.
- Isaac Sim은 이동 결과를 `/amr/odom`으로 AMR Node에 전달한다.

AMR Node가 waypoint 목록을 직접 생성해 Isaac Sim에 보내는 구조는 아니다.

## 8. Pickup과 적재

Pickup 작업의 상태 전이는 다음과 같다.

```text
MOVING_TO_PICKUP
→ ARRIVED_PICKUP
→ LOADING
→ 2초 적재
→ load_state=LOADED
→ LOAD_COMPLETE
```

적재가 완료되면 AMR은 Delivery 좌표를 새로운 `NavigateToPose` goal로 Nav2에
전달한다.

## 9. Delivery와 작업 완료

Delivery 작업의 상태 전이는 다음과 같다.

```text
MOVING_TO_DELIVERY
→ ARRIVED_DELIVERY
→ load_state=EMPTY
→ DELIVERY_COMPLETE
→ MISSION_COMPLETE
→ IDLE
```

현재 정책상 Nav2 결과 대기시간을 초과해도 goal을 자동으로 취소하지 않는다.

## 10. 이동 중 상태 보고

작업 수행 중 AMR은 1초마다 Isaac Sim odom 기준 위치와 현재 상태를 FMS에
전달한다.

```text
event=POSITION_UPDATE
state=MOVING_TO_PICKUP 또는 MOVING_TO_DELIVERY
x=Isaac Sim 현재 위치
y=Isaac Sim 현재 위치
```

FMS는 출발 및 도착 이벤트뿐 아니라 이동 중인 AMR 위치도 확인할 수 있다.

## 11. 다음 작업 요청

Delivery 완료 후 AMR은 `IDLE/EMPTY` 상태로 복귀하고 FMS에 다음 작업을
요청한다.

```text
MISSION_COMPLETE
→ IDLE
→ /fms/request_task 재호출
```

대기 작업이 있으면 FMS가 현재 큐를 다시 cuOpt로 계산해 다음 작업 하나를
할당한다. 작업이 없으면 AMR은 IDLE 상태에서 주기적으로 다시 요청한다.

## 구성요소별 책임

| 구성요소 | 책임 |
|---|---|
| Assembly Cell | 운송 작업 생성 |
| FMS | 작업 큐, AMR 상태, cuOpt 호출, 작업 할당, 논리 위치를 물리 좌표로 변환 |
| cuOpt | 작업 수행 순서 최적화 |
| AMR Node | 작업 Pull, 상태 관리, Nav2 목적지 전달, Isaac Sim 위치 보고 |
| Nav2 | 중간 경로 계산, 경로 추종, `/cmd_vel` 생성 |
| Isaac Sim | AMR 물리 이동 및 `/amr/odom` 제공 |
